TrusTrack

TrusTrack is a mobile app that allows users to share their live location in private circles.

How to Run (VS Code)
Open the project folder in VS Code

Open terminal and run:

flutter pub get
Make sure Firebase is configured (google-services.json added)
Connect an emulator or physical device

Run the app with:

flutter run


Built With

Flutter & Firebase